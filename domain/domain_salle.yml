version: "2.0"

intents:
  - demande_info_salle


responses:
  utter_demande_info_salle:
    - text: "Quel est la salle !"
  

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
